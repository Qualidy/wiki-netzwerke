# 📧 Tag 7: E-Mail-Server (Postfix/Dovecot) - Einfach erklärt



## **📚 Theorie – ganz einfach erklärt**

---



### **1. Wie funktioniert E-Mail?**

Stell dir vor, du schickst einen **Brief**:

1. Du schreibst den Brief und wirfst ihn in einen **Briefkasten** (E-Mail-Client wie Outlook oder Thunderbird).
2. Der **Postbote (SMTP-Server, z. B. Postfix)** holt den Brief ab und bringt ihn zum **Postamt (E-Mail-Server)**.
3. Das Postamt schickt den Brief zum **Postamt des Empfängers** (über das Internet).
4. Der **Postbote des Empfängers (IMAP/POP3-Server, z. B. Dovecot)** legt den Brief in den **Briefkasten des Empfängers** (E-Mail-Postfach).
5. Der Empfänger holt den Brief aus seinem Briefkasten ab (mit einem E-Mail-Client).

**E-Mail-Protokolle:**


| **Protokoll**                               | **Wofür?**                                                        | **Port**           |
| ------------------------------------------- | ----------------------------------------------------------------- | ------------------ |
| **SMTP** (Simple Mail Transfer Protocol)    | **Versenden** von E-Mails.                                        | 25, 587 (mit TLS)  |
| **IMAP** (Internet Message Access Protocol) | **Empfangen und Verwalten** von E-Mails (bleiben auf dem Server). | 143, 993 (mit SSL) |
| **POP3** (Post Office Protocol)             | **Empfangen** von E-Mails (werden heruntergeladen).               | 110, 995 (mit SSL) |




### **2. Postfix – Der E-Mail-Versender**

**Postfix** ist wie ein **Postbote**, der deine E-Mails **versendet**.

- **Aufgabe:** Nimmt E-Mails entgegen und leitet sie an den Empfänger weiter.
- **Vorteile:** Schnell, sicher, einfach zu konfigurieren.


---

### **3. Dovecot – Der E-Mail-Empfänger**

**Dovecot** ist wie ein **Briefkasten**, in dem E-Mails **empfangen und gespeichert** werden.

- **Aufgabe:** Ermöglicht das **Abrufen von E-Mails** (z. B. mit Thunderbird oder einem Handy).
- **Vorteile:** Unterstützt **IMAP und POP3**, sicher, effizient.

---

---

### **4. Wichtige Begriffe**


| **Begriff**     | **Erklärung**                                                    | **Beispiel**       |
| --------------- | ---------------------------------------------------------------- | ------------------ |
| **SMTP-Server** | Server zum **Versenden** von E-Mails.                            | Postfix            |
| **IMAP-Server** | Server zum **Empfangen und Verwalten** von E-Mails.              | Dovecot            |
| **Domain**      | Der Name deines E-Mail-Servers.                                  | `example.com`      |
| **MX-Record**   | Zeigt an, welcher Server für E-Mails einer Domain zuständig ist. | `mail.example.com` |
| **Spam-Filter** | Filtert unerwünschte E-Mails (Spam) heraus.                      | SpamAssassin       |


---

---

## **🎮 Praxis – Postfix und Dovecot installieren**

---

---

### **📌 Schritt 1: Postfix installieren**

```bash
# Postfix installieren
sudo apt update
sudo apt install postfix -y
```

**Während der Installation:**

- Wähle **"Internet Site"**.
- Gib deine **Domain** ein (z. B. `meinmailserver.local` oder eine echte Domain, falls vorhanden).

**Postfix starten und aktivieren:**

```bash
sudo systemctl start postfix
sudo systemctl enable postfix
sudo systemctl status postfix
```

**Erwartete Ausgabe:**

```
● postfix.service - Postfix Mail Transport Agent
   Loaded: loaded (/lib/systemd/system/postfix.service; enabled; vendor preset: enabled)
   Active: active (running) since ...
```

---

---

### **📌 Schritt 2: Dovecot installieren**

```bash
# Dovecot installieren
sudo apt install dovecot-core dovecot-imapd dovecot-pop3d -y
```

**Dovecot starten und aktivieren:**

```bash
sudo systemctl start dovecot
sudo systemctl enable dovecot
sudo systemctl status dovecot
```

**Erwartete Ausgabe:**

```
● dovecot.service - Dovecot IMAP/POP3 email server
   Loaded: loaded (/lib/systemd/system/dovecot.service; enabled; vendor preset: enabled)
   Active: active (running) since ...
```

---

---

### **📌 Schritt 3: Benutzer für E-Mails einrichten**

1. Erstelle einen **Benutzer** (z. B. `testuser`):
  ```bash
   sudo adduser testuser
  ```
  - Gib ein **Passwort** ein (z. B. `Test1234!`).
  - Bestätige die weiteren Fragen mit `Enter`.
2. **E-Mail-Verzeichnis erstellen** (für Dovecot):
  ```bash
   sudo mkdir -p /home/testuser/Maildir
   sudo chown testuser:testuser /home/testuser/Maildir
   sudo chmod 700 /home/testuser/Maildir
  ```

---

---

### **📌 Schritt 4: Postfix für lokale E-Mails konfigurieren**

1. Bearbeite die **Postfix-Konfiguration**:
  ```bash
   sudo nano /etc/postfix/main.cf
  ```
2. Füge folgende Zeilen hinzu oder ändere sie:
  ```ini
   myhostname = mail.meinmailserver.local
   mydomain = meinmailserver.local
   myorigin = $mydomain
   inet_interfaces = all
   mydestination = $myhostname, localhost.$mydomain, localhost, $mydomain
   home_mailbox = Maildir/
  ```
3. Speichern: `Strg + O` → `Enter` → `Strg + X`.
4. **Postfix neu starten:**
  ```bash
   sudo systemctl restart postfix
  ```

---

---

### **📌 Schritt 5: Dovecot konfigurieren**

1. Bearbeite die **Dovecot-Konfiguration**:
  ```bash
   sudo nano /etc/dovecot/conf.d/10-mail.conf
  ```
2. Füge folgende Zeile hinzu oder ändere sie:
  ```ini
   mail_location = maildir:~/Maildir
  ```
3. Speichern und schließen.
4. **Dovecot neu starten:**
  ```bash
   sudo systemctl restart dovecot
  ```

---

---

### **📌 Schritt 6: Firewall für E-Mail-Ports öffnen**

```bash
# SMTP (Port 25 und 587)
sudo ufw allow 25
sudo ufw allow 587

# IMAP (Port 143 und 993)
sudo ufw allow 143
sudo ufw allow 993

# POP3 (Port 110 und 995)
sudo ufw allow 110
sudo ufw allow 995

# Status prüfen
sudo ufw status
```

**Erwartete Ausgabe:**

```
Status: active
To                         Action      From
--                         ------      ----
25/tcp                     ALLOW       Anywhere
587/tcp                    ALLOW       Anywhere
143/tcp                    ALLOW       Anywhere
993/tcp                    ALLOW       Anywhere
110/tcp                    ALLOW       Anywhere
995/tcp                    ALLOW       Anywhere
```

---

---

## **💡 Übungen – einfach & praxisnah**

---

---

### **📝 Aufgabe 1: Postfix installieren und Status prüfen**

**Beschreibung:**

1. Installiere **Postfix** auf deiner Ubuntu-VM.
2. Starte Postfix und prüfe den Status.

**Frage:** Welcher Befehl zeigt an, ob Postfix läuft?

??? success "Lösung:"


    **Befehle:**
    
    ```bash
    sudo apt update
    sudo apt install postfix -y
    sudo systemctl start postfix
    sudo systemctl enable postfix
    sudo systemctl status postfix
    ```
    **Befehl zum Prüfen:**

    - `sudo systemctl status postfix`


---

---

### **📝 Aufgabe 2: Dovecot installieren und Status prüfen**

**Beschreibung:**

1. Installiere **Dovecot** auf deiner Ubuntu-VM.
2. Starte Dovecot und prüfe den Status.

**Frage:** Welcher Befehl zeigt an, ob Dovecot läuft?

??? success "Lösung:"


    **Befehle:**

    ```bash
    sudo apt install dovecot-core dovecot-imapd dovecot-pop3d -y
    sudo systemctl start dovecot
    sudo systemctl enable dovecot
    sudo systemctl status dovecot
    ```
    **Befehl zum Prüfen:**

    - `sudo systemctl status dovecot`


---

### **📝 Aufgabe 3: Benutzer für E-Mails einrichten**

**Beschreibung:**

1. Erstelle einen **Benutzer** mit dem Namen `emailuser`.
2. Erstelle das **Maildir-Verzeichnis** für diesen Benutzer.

**Frage:** Welcher Befehl erstellt ein neues Verzeichnis?

??? success "Lösung:"


    **Benutzer erstellen:**

    ```bash
    sudo adduser emailuser
    ```
    **Maildir-Verzeichnis erstellen:**

    ```bash
    sudo mkdir -p /home/emailuser/Maildir
    sudo chown emailuser:emailuser /home/emailuser/Maildir
    sudo chmod 700 /home/emailuser/Maildir
    ```
    **Befehl zum Erstellen eines Verzeichnisses:**

    - `sudo mkdir -p /home/emailuser/Maildir`


---



### **📝 Aufgabe 4: E-Mail zwischen zwei VMs senden**

**Beschreibung:**

1. Richte auf **zwei VMs** Postfix und Dovecot ein (wie oben beschrieben).
2. Sende eine **E-Mail von VM1 an VM2** mit dem `mail`-Befehl:
  ```bash
   echo "Hallo, das ist eine Test-E-Mail!" | mail -s "Test-E-Mail" emailuser@IP_DER_VM2
  ```
  - Ersetze `IP_DER_VM2` mit der **IP-Adresse der zweiten VM**.
3. Melde dich auf **VM2** als `emailuser` an und prüfe die E-Mails:
  ```bash
   su - emailuser
   mail
  ```

**Frage:** Welcher Befehl zeigt die E-Mails von `emailuser` an?

??? success "Lösung:"


    **E-Mail senden (auf VM1):**

    ```bash
    echo "Hallo, das ist eine Test-E-Mail!" | mail -s "Test-E-Mail" emailuser@192.168.1.2
    ```
    **E-Mails abrufen (auf VM2):**

    ```bash
    su - emailuser
    mail
    ```
    **Befehl zum Anzeigen der E-Mails:**

    - `mail` (nach dem Einloggen als `emailuser`)

---

---

### **📝 Aufgabe 5: Postfix-Konfiguration anpassen**

**Beschreibung:**

1. Bearbeite die Datei `/etc/postfix/main.cf`.
2. Ändere die Zeile `myhostname` in `mail.meinmailserver.local`.
3. Starte Postfix neu.

**Frage:** Welcher Befehl startet Postfix neu?

??? success "Lösung:"


    **Konfiguration bearbeiten:**

    ```bash
    sudo nano /etc/postfix/main.cf
    ```
    **Zeile anpassen:**

    ```ini
    myhostname = mail.meinmailserver.local
    ```
    **Postfix neu starten:**

    ```bash
    sudo systemctl restart postfix
    ```
    **Befehl zum Neustarten:**

    - `sudo systemctl restart postfix`




### **📝 Aufgabe 6: Firewall für E-Mail-Ports öffnen**

**Beschreibung:**

1. Erlaube die **Ports 25, 143 und 587** in der Firewall.
2. Prüfe den Status der Firewall.

**Frage:** Welcher Befehl zeigt den Status der Firewall an?

??? success "Lösung:"


    **Ports freigeben:**

    ```bash
    sudo ufw allow 25
    sudo ufw allow 143
    sudo ufw allow 587
    ```
    **Status prüfen:**

    ```bash
    sudo ufw status
    ```
    **Befehl zum Anzeigen des Firewall-Status:**

    - `sudo ufw status`


---

---

### **📝 Aufgabe 7: E-Mail mit Telnet testen (für Fortgeschrittene)**

**Beschreibung:**

1. Verbinde dich mit **Telnet** zu deinem Postfix-Server (Port 25):
  ```bash
   telnet localhost 25
  ```
2. Gib folgende Befehle ein (ersetze die E-Mail-Adressen):
  ```
   EHLO meinmailserver.local
   MAIL FROM: testuser@meinmailserver.local
   RCPT TO: emailuser@meinmailserver.local
   DATA
   Subject: Test-E-Mail

   Das ist eine Test-E-Mail.
   .
   QUIT
  ```
3. Prüfe auf dem Empfänger, ob die E-Mail angekommen ist.

**Frage:** Welcher Befehl verbindet dich mit dem SMTP-Server?

??? success "Lösung:"


    **Verbindung mit Telnet herstellen:**

    ```bash
    telnet localhost 25
    ```
    **Befehl zum Verbinden mit dem SMTP-Server:**

    - `telnet localhost 25`


---

---

## **🎓 Zusammenfassung – das Wichtigste**


| **Thema**   | **Was es macht**                                         | **Beispiel**                    |
| ----------- | -------------------------------------------------------- | ------------------------------- |
| **Postfix** | Versendet E-Mails (SMTP-Server).                         | `sudo apt install postfix`      |
| **Dovecot** | Empfängt und verwaltet E-Mails (IMAP/POP3-Server).       | `sudo apt install dovecot-core` |
| **SMTP**    | Protokoll zum **Versenden** von E-Mails.                 | Port 25, 587                    |
| **IMAP**    | Protokoll zum **Empfangen und Verwalten** von E-Mails.   | Port 143, 993                   |
| **POP3**    | Protokoll zum **Empfangen** von E-Mails (Herunterladen). | Port 110, 995                   |
| **Maildir** | Verzeichnis, in dem E-Mails gespeichert werden.          | `/home/benutzer/Maildir`        |


**Merksätze:**

1. **Postfix = Versenden, Dovecot = Empfangen**.
2. **SMTP = Senden, IMAP/POP3 = Empfangen**.
3. **Port 25 = SMTP, Port 143 = IMAP, Port 110 = POP3**.
4. **E-Mails werden in `/home/benutzer/Maildir` gespeichert**.
