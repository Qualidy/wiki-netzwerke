# 🌐 Tag 3: IP-Adressierung & Subnetting



## **📖 Theorie**



### **1. Einführung in IP-Adressierung**

Eine **IP-Adresse** (Internet Protocol Address) ist eine **eindeutige numerische Adresse**, die jedem Gerät in einem Netzwerk zugewiesen wird, um die **Kommunikation** zu ermöglichen.

#### **Zweck von IP-Adressen**

- **Identifikation:** Eindeutige Kennung eines Geräts im Netzwerk.
- **Lokalisierung:** Bestimmung des **Standorts** eines Geräts (z. B. in einem Subnetz).
- **Routing:** Weiterleitung von Datenpaketen durch das Netzwerk.

---

---

### **2. IPv4 vs. IPv6**


| **Kriterium**       | **IPv4**                        | **IPv6**                                             |
| ------------------- | ------------------------------- | ---------------------------------------------------- |
| **Adresslänge**     | 32 Bit (4 Bytes)                | 128 Bit (16 Bytes)                                   |
| **Anzahl Adressen** | ~4,3 Mrd. (2³²)                 | ~340 Sextillionen (2¹²⁸)                             |
| **Format**          | Dezimal (z. B. `192.168.1.1`)   | Hexadezimal (z. B. `2001:0db8:85a3::8a2e:0370:7334`) |
| **Verwendung**      | Dominierend (90% des Internets) | Zunehmend (z. B. für IoT, Mobilfunk)                 |
| **Vorteile**        | Einfach, weit verbreitet        | Kein NAT nötig, mehr Adressen, bessere Sicherheit    |
| **Nachteile**       | Adressknappheit, NAT nötig      | Komplexere Konfiguration                             |


#### **IPv4-Adressformat**

- **4 Oktette** (jeweils 8 Bit), durch Punkte getrennt.
- **Beispiel:** `192.168.1.1`
    - **Oktett 1:** `192` (Netzwerkanteil)
    - **Oktett 2:** `168` (Netzwerkanteil)
    - **Oktett 3:** `1` (Subnetzanteil)
    - **Oktett 4:** `1` (Hostanteil)

#### **IPv6-Adressformat**

- **8 Blöcke** à 16 Bit, durch Doppelpunkte getrennt.
- **Beispiel:** `2001:0db8:85a3:0000:0000:8a2e:0370:7334`
  - **Verkürzte Schreibweise:**
    - Führende Nullen können weggelassen werden (`2001:db8:85a3:0:0:8a2e:370:7334`).
    - **Doppelte Nullen** können durch `::` ersetzt werden (`2001:db8:85a3::8a2e:370:7334`).

---

---

### **3. IP-Adressklassen (IPv4)**

IPv4-Adressen werden in **5 Klassen** unterteilt, die den **Verwendungszweck** bestimmen.


| **Klasse** | **Bereich**                 | **Erstes Bit** | **Verwendung**                           | **Standard-Subnetzmaske** |
| ---------- | --------------------------- | -------------- | ---------------------------------------- | ------------------------- |
| **A**      | 1.0.0.0 – 126.255.255.255   | 0              | Große Netzwerke (z. B. Regierungen)      | 255.0.0.0 (/8)            |
| **B**      | 128.0.0.0 – 191.255.255.255 | 10             | Mittlere Netzwerke (z. B. Universitäten) | 255.255.0.0 (/16)         |
| **C**      | 192.0.0.0 – 223.255.255.255 | 110            | Kleine Netzwerke (z. B. Unternehmen)     | 255.255.255.0 (/24)       |
| **D**      | 224.0.0.0 – 239.255.255.255 | 1110           | **Multicast** (Gruppenkommunikation)     | –                         |
| **E/R**      | 240.0.0.0 – 255.255.255.255 | 11110           | Reserved (für zukünftige Verwendung)    | –                         |





#### **Spezielle IP-Adressen**


| **Adresse**       | **Bedeutung**                                                         |
| ----------------- | --------------------------------------------------------------------- |
| `0.0.0.0`         | **Standard-Route** (Default Gateway)                                  |
| `127.0.0.1`       | **Loopback** (lokaler Host)                                           |
| `255.255.255.255` | **Broadcast** (alle Geräte im Netzwerk)                               |
| `169.254.x.x`     | **APIPA** (Automatische private IP-Zuweisung, falls DHCP fehlschlägt) |


---

---

### **4. Subnetting – Warum und wie?**

**Subnetting** ist die **Aufteilung eines Netzwerks in kleinere Teilnetze (Subnetze)**, um:

- **IP-Adressen effizienter zu nutzen** (Vermeidung von Adressverschwendung).
- **Netzwerkverkehr zu reduzieren** (geringere Broadcast-Domänen).
- **Sicherheit zu erhöhen** (Isolation von Netzwerksegmenten).
- **Routing zu vereinfachen** (bessere Kontrolle über Datenflüsse).

#### **Subnetzmaske**

- Die **Subnetzmaske** bestimmt, welcher Teil der IP-Adresse zum **Netzwerk** und welcher zum **Host** gehört.
- **Beispiel:**
    - IP: `192.168.1.10`
    - Subnetzmaske: `255.255.255.0` (`/24`)
    - **Netzwerkanteil:** `192.168.1`
    - **Hostanteil:** `.10`

#### **CIDR-Notation (Classless Inter-Domain Routing)**

- **Ersetzt die klassische Klassenunterteilung** (A, B, C).
- **Format:** `IP-Adresse/Präfixlänge` (z. B. `192.168.1.0/24`).
- **Präfixlänge:** Anzahl der Bits für den **Netzwerkanteil** (z. B. `/24` = 24 Bits für Netzwerk, 8 Bits für Hosts).


| **Subnetzmaske** | **CIDR-Notation** | **Anzahl Hosts** |
| ---------------- | ----------------- | ---------------- |
| 255.0.0.0        | /8                | 16.777.214       |
| 255.255.0.0      | /16               | 65.534           |
| 255.255.255.0    | /24               | 254              |
| 255.255.255.128  | /25               | 126              |
| 255.255.255.192  | /26               | 62               |
| 255.255.255.224  | /27               | 30               |
| 255.255.255.240  | /28               | 14               |


#### **Berechnung der Anzahl Hosts pro Subnetz**

- **Formel:** `2^(Anzahl Host-Bits) - 2`
    - **-2**, weil **Netzwerkadresse** und **Broadcast-Adresse** nicht für Hosts genutzt werden können.
- **Beispiel für /24:**
    - Host-Bits: `32 - 24 = 8`
    - Anzahl Hosts: `2^8 - 2 = 256 - 2 = 254`

---

---

### **5. Subnetting Schritt für Schritt**

#### **Beispiel: Netzwerk `192.168.1.0/24` in 4 Subnetze aufteilen**

1. **Anforderung:**
    - 4 Subnetze mit je **mindestens 30 Hosts**.
2. **Host-Bits berechnen:**
    - 30 Hosts → `2^n - 2 ≥ 30` → `n = 5` (2⁵ - 2 = 30).
3. **Netzwerk-Bits:**
    - Ursprünglich: `/24` (24 Bits für Netzwerk).
    - Neue Präfixlänge: `32 - 5 = 27` → **/27**.
4. **Subnetzmaske:**
    - `/27` → `255.255.255.224`.
5. **Subnetze berechnen:**
    - **Subnetz 1:** `192.168.1.0/27` (Hosts: `192.168.1.1` – `192.168.1.30`)
    - **Subnetz 2:** `192.168.1.32/27` (Hosts: `192.168.1.33` – `192.168.1.62`)
    - **Subnetz 3:** `192.168.1.64/27` (Hosts: `192.168.1.65` – `192.168.1.94`)
    - **Subnetz 4:** `192.168.1.96/27` (Hosts: `192.168.1.97` – `192.168.1.126`)

#### **Visualisierung**


| **Subnetz** | **Netzwerkadresse** | **Erste Host-IP** | **Letzte Host-IP** | **Broadcast-Adresse** |
| ----------- | ------------------- | ----------------- | ------------------ | --------------------- |
| Subnetz 1   | 192.168.1.0/27      | 192.168.1.1       | 192.168.1.30       | 192.168.1.31          |
| Subnetz 2   | 192.168.1.32/27     | 192.168.1.33      | 192.168.1.62       | 192.168.1.63          |
| Subnetz 3   | 192.168.1.64/27     | 192.168.1.65      | 192.168.1.94       | 192.168.1.95          |
| Subnetz 4   | 192.168.1.96/27     | 192.168.1.97      | 192.168.1.126      | 192.168.1.127         |


---

---

### **6. VLSM (Variable Length Subnet Masking)**

- **Problem:** Bei klassischem Subnetting werden **alle Subnetze gleich groß** erstellt (z. B. 4x /27).
- **Lösung:** **VLSM** ermöglicht **unterschiedlich große Subnetze** innerhalb eines Netzwerks.
- **Beispiel:**
    - Netzwerk: `192.168.1.0/24`
    - **Subnetz 1:** 50 Hosts → `/26` (62 Hosts)
    - **Subnetz 2:** 20 Hosts → `/27` (30 Hosts)
    - **Subnetz 3:** 10 Hosts → `/28` (14 Hosts)

#### **VLSM-Berechnung**

1. **Größtes Subnetz zuerst** (Top-Down-Ansatz).
2. **Subnetz 1 (50 Hosts):**
    - `/26` → `192.168.1.0/26` (Hosts: `192.168.1.1` – `192.168.1.62`).
3. **Subnetz 2 (20 Hosts):**
    - `/27` → `192.168.1.64/27` (Hosts: `192.168.1.65` – `192.168.1.94`).
4. **Subnetz 3 (10 Hosts):**
    - `/28` → `192.168.1.96/28` (Hosts: `192.168.1.97` – `192.168.1.110`).

---

---

### **7. Tools für Subnetting**

#### `**ipcalc` (Linux)**

- **Installation:**
  ```bash
  sudo apt install ipcalc -y
  ```
- **Beispiel:**
  ```bash
  ipcalc 192.168.1.0/24
  ```
  **Ausgabe:**
  ```
  Address:   192.168.1.0        11000000.10101000.00000001.00000000
  Netmask:   255.255.255.0 = 24 11111111.11111111.11111111.00000000
  Wildcard:  0.0.0.255        00000000.00000000.00000000.11111111
  =>
  Network:   192.168.1.0/24    11000000.10101000.00000001.00000000
  HostMin:   192.168.1.1       11000000.10101000.00000001.00000001
  HostMax:   192.168.1.254     11000000.10101000.00000001.11111110
  Broadcast: 192.168.1.255     11000000.10101000.00000001.11111111
  Hosts/Net: 254               (Private Internet)
  ```

#### **Online-Tools**

- [Subnet Calculator](https://www.calculator.net/ip-subnet-calculator.html)


---

---

## **💡 Übungsaufgaben**

---

### **📝 Aufgabe 1: IP-Adressklassen zuordnen**

**Beschreibung:**  
Ordne die folgenden IP-Adressen den **richtigen Klassen (A, B, C, D, E)** zu.


| **IP-Adresse** | **Klasse** |
| -------------- | ---------- |
| 10.5.20.30     | ?          |
| 172.16.254.1   | ?          |
| 192.168.1.1    | ?          |
| 224.0.0.5      | ?          |
| 240.1.1.1      | ?          |
| 128.10.0.1     | ?          |


??? success "Lösung:"




    | **IP-Adresse** | **Klasse** |
    | -------------- | ---------- |
    | 10.5.20.30     | A          |
    | 172.16.254.1   | B          |
    | 192.168.1.1    | C          |
    | 224.0.0.5      | D          |
    | 240.1.1.1      | E          |
    | 128.10.0.1     | B          |


&nbsp;

---



### **📝 Aufgabe 3: Subnetzmaske in CIDR umwandeln**

**Beschreibung:**  
Wandle die folgenden **Subnetzmasken** in **CIDR-Notation** um.


| **Subnetzmaske** | **CIDR** |
| ---------------- | -------- |
| 255.0.0.0        | ?        |
| 255.255.0.0      | ?        |
| 255.255.255.0    | ?        |
| 255.255.255.128  | ?        |
| 255.255.255.192  | ?        |
| 255.255.255.240  | ?        |


??? success "Lösung:"


    | **Subnetzmaske** | **CIDR** |
    | ---------------- | -------- |
    | 255.0.0.0        | /8       |
    | 255.255.0.0      | /16      |
    | 255.255.255.0    | /24      |
    | 255.255.255.128  | /25      |
    | 255.255.255.192  | /26      |
    | 255.255.255.240  | /28      |


&nbsp;

---

---

### **📝 Aufgabe 4: Anzahl Hosts pro Subnetz berechnen**

**Beschreibung:**  
Berechne die **Anzahl der nutzbaren Hosts** für die folgenden Subnetze.


| **CIDR** | **Anzahl Hosts** |
| -------- | ---------------- |
| /24      | ?                |
| /25      | ?                |
| /26      | ?                |
| /27      | ?                |
| /28      | ?                |
| /30      | ?                |

??? success "Lösung:"

    | **CIDR** | **Anzahl Hosts** |
    | -------- | ---------------- |
    | /24      | 254              |
    | /25      | 126              |
    | /26      | 62               |
    | /27      | 30               |
    | /28      | 14               |
    | /30      | 2                |


&nbsp;





### **📝 Aufgabe 5: Praktische Übung mit `ipcalc**`

**Beschreibung:**

1. Installiere `ipcalc` auf deiner Ubuntu-VM:
  ```bash
   sudo apt install ipcalc -y
  ```
2. Führe folgende Befehle aus und interpretiere die Ausgaben:
  ```bash
   ipcalc 192.168.1.0/24
   ipcalc 10.0.0.0/16
   ipcalc 172.16.0.0/20
  ```
3. **Frage:** Wie viele Hosts können in einem `/20`-Netzwerk genutzt werden?


??? success "Lösung:"

    1. **Ausgabe für `192.168.1.0/24`:**

    ```
    Network:   192.168.1.0/24
    HostMin:   192.168.1.1
    HostMax:   192.168.1.254
    Hosts/Net: 254
    ```
    2. **Ausgabe für `10.0.0.0/16`:**

    ```
    Network:   10.0.0.0/16
    HostMin:   10.0.0.1
    HostMax:   10.0.255.254
    Hosts/Net: 65534
    ```
    3. **Ausgabe für `172.16.0.0/20`:**

    ```
    Network:   172.16.0.0/20
    HostMin:   172.16.0.1
    HostMax:   172.16.15.254
    Hosts/Net: 4094
    ```
    4. **Anzahl Hosts in `/20`:**

      - `2^(32-20) - 2 = 2^12 - 2 = 4096 - 2 = 4094`.

&nbsp;


 

# 🌐 Minimales VLAN-Labor mit Netplan


### **2. Unser Labor-Aufbau**

Wir bauen ein **Mini-Netzwerk mit 2 VLANs** auf:

```
[Router-VM]
│   ├── eth0.10 → 192.168.10.1 (VLAN 10)
│   └── eth0.20 → 192.168.20.1 (VLAN 20)
│
[VM1] → 192.168.10.10 (VLAN 10)
[VM2] → 192.168.20.10 (VLAN 20)
```

**Ziel:**

- VM1 und VM2 **können sich nicht direkt** erreichen (unterschiedliche VLANs).
- Der **Router** entscheidet, ob Verkehr zwischen den VLANs erlaubt ist.

---

---

---

## 🛠️ **Praxis: Schritt-für-Schritt-Anleitung**

---

---

### **📌 Schritt 1: Netzwerk-Topologie vorbereiten**

**Voraussetzung:**

- **3 Ubuntu-VMs** (Router-VM, VM1, VM2) in VirtualBox/VMware mit **Host-Only-Netzwerk**.
- Alle VMs sind **miteinander verbunden** (z. B. über ein internes Netzwerk).

**Empfohlene VM-Konfiguration:**


| **VM**   | **Name**  | **RAM** | **CPU** | **Festplatte** | **Netzwerk-Interface** |
| -------- | --------- | ------- | ------- | -------------- | ---------------------- |
| Router   | Router-VM | 1 GB    | 1 Kern  | 10 GB          | eth0                   |
| Client 1 | VM1       | 1 GB    | 1 Kern  | 10 GB          | eth0                   |
| Client 2 | VM2       | 1 GB    | 1 Kern  | 10 GB          | eth0                   |


---

---

### **📌 Schritt 2: Router-VM konfigurieren**

**Ziel:** Die Router-VM soll **zwei VLANs (10 und 20)** verwalten.

#### **1. VLAN-Subinterfaces in Netplan erstellen**

Auf der **Router-VM**:

```bash
sudo nano /etc/netplan/01-netcfg.yaml
```

**Inhalt:**

```yaml
network:
  version: 2
  renderer: networkd
  ethernets:
    eth0:
      dhcp4: no
  vlans:
    eth0.10:          # VLAN 10
      id: 10           # VLAN-ID 10
      link: eth0       # Basiert auf eth0
      addresses: [192.168.10.1/24]  # IP für VLAN 10
    eth0.20:          # VLAN 20
      id: 20           # VLAN-ID 20
      link: eth0
      addresses: [192.168.20.1/24]  # IP für VLAN 20
```

**Was bedeutet das?**


| **Teil**                       | **Erklärung**                                                         |
| ------------------------------ | --------------------------------------------------------------------- |
| `eth0.10`                      | Erstellt ein **Subinterface** für VLAN 10 auf `eth0`.                 |
| `id: 10`                       | **VLAN-ID 10** – alle Pakete mit VLAN-Tag 10 werden hier verarbeitet. |
| `addresses: [192.168.10.1/24]` | Weist die **IP 192.168.10.1** diesem Subinterface zu.                 |


**Konfiguration anwenden:**

```bash
sudo netplan apply
```

#### **2. IP-Forwarding aktivieren**

```bash
# Temporär aktivieren
sudo sysctl -w net.ipv4.ip_forward=1

# Dauerhaft aktivieren
echo "net.ipv4.ip_forward=1" | sudo tee -a /etc/sysctl.conf
sudo sysctl -p
```

**Was passiert hier?**

- **IP-Forwarding** ermöglicht es der Router-VM, **Pakete zwischen den VLANs weiterzuleiten**.

---

---

### **📌 Schritt 3: VM1 (VLAN 10) konfigurieren**

**Ziel:** VM1 bekommt eine IP in **VLAN 10**.

```bash
sudo nano /etc/netplan/01-netcfg.yaml
```

**Inhalt:**

```yaml
network:
  version: 2
  renderer: networkd
  ethernets:
    eth0:
      addresses: [192.168.10.10/24]  # IP für VM1 in VLAN 10
      gateway4: 192.168.10.1          # Gateway = Router-VM
      nameservers:
        addresses: [8.8.8.8, 8.8.4.4] # DNS-Server
```

**Konfiguration anwenden:**

```bash
sudo netplan apply
```

---

---

### **📌 Schritt 4: VM2 (VLAN 20) konfigurieren**

**Ziel:** VM2 bekommt eine IP in **VLAN 20**.

```bash
sudo nano /etc/netplan/01-netcfg.yaml
```

**Inhalt:**

```yaml
network:
  version: 2
  renderer: networkd
  ethernets:
    eth0:
      addresses: [192.168.20.10/24]  # IP für VM2 in VLAN 20
      gateway4: 192.168.20.1          # Gateway = Router-VM
      nameservers:
        addresses: [8.8.8.8, 8.8.4.4]
```

**Konfiguration anwenden:**

```bash
sudo netplan apply
```

---

---

### **📌 Schritt 5: Firewall-Regeln auf der Router-VM**

**Ziel:** Standardmäßig **allen Verkehr zwischen den VLANs blockieren**.

```bash
# Alle Verbindungen zwischen VLAN 10 und VLAN 20 blockieren
sudo iptables -A FORWARD -i eth0.10 -o eth0.20 -j DROP
sudo iptables -A FORWARD -i eth0.20 -o eth0.10 -j DROP

# Regeln speichern (für Ubuntu/Debian)
sudo apt install iptables-persistent -y
sudo netfilter-persistent save
```

**Was passiert hier?**

- `**iptables -A FORWARD -i eth0.10 -o eth0.20 -j DROP**` blockiert alle Pakete von VLAN 10 zu VLAN 20.
- `**iptables -A FORWARD -i eth0.20 -o eth0.10 -j DROP**` blockiert alle Pakete von VLAN 20 zu VLAN 10.

---

---

### **📌 Schritt 6: Testen**

#### **1. Ping innerhalb eines VLANs (sollte funktionieren)**

- **Von VM1 zu sich selbst:**
  ```bash
  ping 192.168.10.10
  ```
  → **Funktioniert!** (Lokaler Ping)

#### **2. Ping zwischen VLANs (sollte NICHT funktionieren)**

- **Von VM1 (VLAN 10) zu VM2 (VLAN 20):**
  ```bash
  ping 192.168.20.10
  ```
  → **Funktioniert NICHT!** (Firewall blockiert den Verkehr.)

#### **3. Ping zwischen VLANs erlauben (optional)**

Falls du den Verkehr zwischen den VLANs **erlauben** willst:

```bash
# Erlaube Ping (ICMP) zwischen VLAN 10 und VLAN 20
sudo iptables -A FORWARD -i eth0.10 -o eth0.20 -p icmp -j ACCEPT
sudo iptables -A FORWARD -i eth0.20 -o eth0.10 -p icmp -j ACCEPT
sudo netfilter-persistent save
```

→ **Jetzt funktioniert der Ping zwischen VM1 und VM2!**

---

---

---

## 💡 **Übungen für die Teilnehmer**

---

---

### **📝 Aufgabe 1: Router-VM konfigurieren**

**Beschreibung:**

1. Richte auf der Router-VM **VLAN-Subinterfaces** für VLAN 10 und VLAN 20 ein.
  - VLAN 10: `192.168.10.1/24`
  - VLAN 20: `192.168.20.1/24`
2. Aktiviere **IP-Forwarding** auf der Router-VM.
3. Prüfe mit `ip a`, ob die VLAN-Interfaces existieren.

**Frage:** Welche IP-Adresse hat das Interface `eth0.10` auf der Router-VM?

??? success "Lösung:"

```
**Konfiguration in `/etc/netplan/01-netcfg.yaml`:**
```yaml
network:
  version: 2
  renderer: networkd
  ethernets:
    eth0:
      dhcp4: no
  vlans:
    eth0.10:
      id: 10
      link: eth0
      addresses: [192.168.10.1/24]
    eth0.20:
      id: 20
      link: eth0
      addresses: [192.168.20.1/24]
```
**IP-Forwarding aktivieren:**
```bash
sudo sysctl -w net.ipv4.ip_forward=1
echo "net.ipv4.ip_forward=1" | sudo tee -a /etc/sysctl.conf
sudo sysctl -p
sudo netplan apply
```

**Antwort:**
Die IP-Adresse von `eth0.10` ist **`192.168.10.1/24`**.
```

---

---

### **📝 Aufgabe 2: VMs konfigurieren und testen**

**Beschreibung:**

1. Konfiguriere **VM1** mit der IP `192.168.10.10/24` und Gateway `192.168.10.1`.
2. Konfiguriere **VM2** mit der IP `192.168.20.10/24` und Gateway `192.168.20.1`.
3. Teste die Verbindung von VM1 zu VM2 mit `ping`.

**Frage:** Warum funktioniert der Ping von VM1 zu VM2 **nicht**?

??? success "Lösung:"

```
**Konfiguration für VM1 (`/etc/netplan/01-netcfg.yaml`):**
```yaml
network:
  version: 2
  renderer: networkd
  ethernets:
    eth0:
      addresses: [192.168.10.10/24]
      gateway4: 192.168.10.1
      nameservers:
        addresses: [8.8.8.8, 8.8.4.4]
```

**Konfiguration für VM2 (`/etc/netplan/01-netcfg.yaml`):**
```yaml
network:
  version: 2
  renderer: networkd
  ethernets:
    eth0:
      addresses: [192.168.20.10/24]
      gateway4: 192.168.20.1
      nameservers:
        addresses: [8.8.8.8, 8.8.4.4]
```

**Ping-Test:**
```bash
ping 192.168.20.10
```

**Antwort:**
Der Ping funktioniert **nicht**, weil:
- VM1 (`192.168.10.10`) und VM2 (`192.168.20.10`) sind in **unterschiedlichen VLANs** (10 und 20).
- Die **Firewall auf der Router-VM blockiert** den Verkehr zwischen den VLANs.
```

---

---

### **📝 Aufgabe 3: Firewall-Regeln anpassen**

**Beschreibung:**

1. Erlaube auf der Router-VM **Ping (ICMP)** zwischen VLAN 10 und VLAN 20.
2. Teste erneut, ob VM1 jetzt VM2 pingen kann.

**Frage:** Welcher Befehl erlaubt ICMP-Verkehr zwischen den VLANs?

??? success "Lösung:"

```
**Firewall-Regeln für ICMP:**
```bash
sudo iptables -A FORWARD -i eth0.10 -o eth0.20 -p icmp -j ACCEPT
sudo iptables -A FORWARD -i eth0.20 -o eth0.10 -p icmp -j ACCEPT
sudo netfilter-persistent save
```

**Antwort:**
```bash
sudo iptables -A FORWARD -i eth0.10 -o eth0.20 -p icmp -j ACCEPT
sudo iptables -A FORWARD -i eth0.20 -o eth0.10 -p icmp -j ACCEPT
```
```

---

---

### **📝 Aufgabe 4: HTTP-Verkehr erlauben (optional)**

**Beschreibung:**

1. Installiere **Apache** auf VM1.
2. Erstelle eine einfache Webseite (`/var/www/html/index.html`).
3. Erlaube auf der Router-VM **HTTP-Verkehr (Port 80)** von VLAN 20 zu VLAN 10.
4. Teste von VM2 aus, ob die Webseite von VM1 erreichbar ist:
  ```bash
   curl http://192.168.10.10
  ```

**Frage:** Welche Firewall-Regel erlaubt HTTP-Verkehr von VLAN 20 zu VLAN 10?

??? success "Lösung:"

```
**Apache installieren (auf VM1):**
```bash
sudo apt update
sudo apt install apache2 -y
sudo systemctl start apache2
sudo systemctl enable apache2
```

**Einfache Webseite erstellen:**
```bash
sudo nano /var/www/html/index.html
```
```html
<!DOCTYPE html>
<html>
<body>
    <h1>Willkommen auf VM1!</h1>
    <p>Das ist eine Testseite in VLAN 10.</p>
</body>
</html>
```

**Firewall-Regel auf der Router-VM:**
```bash
sudo iptables -A FORWARD -i eth0.20 -o eth0.10 -p tcp --dport 80 -j ACCEPT
sudo netfilter-persistent save
```

**Antwort:**
```bash
sudo iptables -A FORWARD -i eth0.20 -o eth0.10 -p tcp --dport 80 -j ACCEPT
```
```

---

---

---

## 🎓 **Zusammenfassung**

### **Was du gelernt hast:**

✅ **VLANs** trennen Netzwerke logisch, auch wenn sie physisch verbunden sind.  
✅ **Netplan** kann **VLAN-Subinterfaces** einfach konfigurieren.  
✅ **IP-Forwarding** ermöglicht es einem Router, Pakete zwischen Netzwerken weiterzuleiten.  
✅ **iptables** kann Verkehr zwischen VLANs **blockieren oder erlauben**.

### **Wichtige Befehle:**


| **Zweck**                      | **Befehl**                                                      |
| ------------------------------ | --------------------------------------------------------------- |
| VLAN-Subinterface erstellen    | `eth0.10: { id: 10, link: eth0, addresses: [192.168.10.1/24] }` |
| IP-Forwarding aktivieren       | `sudo sysctl -w net.ipv4.ip_forward=1`                          |
| Firewall-Regel hinzufügen      | `sudo iptables -A FORWARD -i eth0.10 -o eth0.20 -j DROP`        |
| Firewall-Regeln speichern      | `sudo netfilter-persistent save`                                |
| Netplan-Konfiguration anwenden | `sudo netplan apply`                                            |


---

---

### **Typische Fehler & Lösungen:**


| **Problem**                            | **Ursache**                   | **Lösung**                                                         |
| -------------------------------------- | ----------------------------- | ------------------------------------------------------------------ |
| VLANs funktionieren nicht              | IP-Forwarding ist deaktiviert | `sudo sysctl -w net.ipv4.ip_forward=1`                             |
| VMs können sich nicht verbinden        | Falsche IP oder Gateway       | Prüfe die Netplan-Konfiguration mit `ip a`                         |
| Firewall blockiert alles               | Standardrichtlinie ist `DROP` | Erlaube explizit den gewünschten Verkehr                           |
| Ping funktioniert nicht zwischen VLANs | Firewall-Regel blockiert ICMP | `sudo iptables -A FORWARD -i eth0.10 -o eth0.20 -p icmp -j ACCEPT` |


---

---

## 📌 **Hausaufgabe & Vertiefung**

### **📝 Aufgabe 1: Drittes VLAN hinzufügen**

**Beschreibung:**

1. Füge ein **drittes VLAN (VLAN 30)** mit dem Netzwerk `192.168.30.0/24` hinzu.
2. Erstelle eine **dritte VM (VM3)** und weise sie VLAN 30 zu.
3. Konfiguriere die **Router-VM**, damit sie auch VLAN 30 bedient.
4. Erlaube **Ping zwischen VLAN 20 und VLAN 30**, aber **blockiere Ping zwischen VLAN 10 und VLAN 30**.

**Frage:** Welche Firewall-Regeln musst du auf der Router-VM hinzufügen?

---

---

### **📝 Aufgabe 2: Netzwerkdiagramm erstellen**

**Beschreibung:**

1. Erstelle ein **Netzwerkdiagramm** (z. B. mit [draw.io](https://app.diagrams.net/)) deines Setups.
2. Zeige darin:
  - Die **Router-VM** mit ihren VLAN-Subinterfaces.
  - Die **VMs** und ihre Zuordnung zu den VLANs.
  - Die **Firewall-Regeln** zwischen den VLANs.

---

---

## 🔗 **Nützliche Links**

- [Netplan Dokumentation](https://netplan.io/)
- [iptables Tutorial](https://www.netfilter.org/documentation/HOWTO//netfilter-hacking-HOWTO.html)
- [VLANs einfach erklärt](https://de.wikipedia.org/wiki/Virtual_LAN)

---

---

## 💬 **Feedback & Fragen**

❓ **Hast du noch Fragen?** – Frag einfach!  
❓ **Was war unklar?** – Ich erkläre es gerne nochmal!  
❓ **Möchtest du mehr üben?** – Ich habe weitere Aufgaben für dich!

**Viel Erfolg mit deinem minimalen VLAN-Labor!** 🚀